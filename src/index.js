import React from "react"
//when importing ReactDOM remember it has to be from "react-dom"
import ReactDOM from "react-dom"
import App from "./App"
import "./style.css"

ReactDOM.render(<App />, document.getElementById("root"))